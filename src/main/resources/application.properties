spring.application.name=agendamento-app
server.port=8081

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:8745/agendamento_db
spring.datasource.username=postgres
spring.datasource.password=Vrpost@Server
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
#spring.jpa.hibernate.ddl-auto=create
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway (migrações)
spring.flyway.enabled=false

# Logging
logging.level.com.example.agendamento_app=DEBUG
logging.level.org.springframework.web=INFO

# Desabilita Security completamente
spring.security.enabled=false